#include<iostream>

using namespace std;
int mod = 233;
void powe(int n, int base)
{
	int ans = 1;
	while(n)
	{
		if(n%2==0)
		{
			base = (base*base) % mod;
			n /=2;
		}
		else
		{
			ans = (ans*base) % mod;
			n -= 1;
		}
	}
	cout<<ans<<"\n";
}
int main()
{
	int power ,base;
	cin>>base>>power;
	powe(power,base);
	return 0;
}
